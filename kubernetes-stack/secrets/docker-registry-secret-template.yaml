# Template for Docker Registry Secret
# Replace the placeholder values with your actual base64 encoded credentials
# Use the generate-docker-config.sh script to create the actual secret

apiVersion: v1
kind: Secret
metadata:
  name: docker-registry-secret
  namespace: canopy
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: PLACEHOLDER_BASE64_CONFIG_HERE
---
apiVersion: v1
kind: Secret
metadata:
  name: docker-registry-secret
  namespace: monitoring
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: PLACEHOLDER_BASE64_CONFIG_HERE 
